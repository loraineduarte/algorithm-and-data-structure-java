public class exe2 {
	/*
	List<Integer> lista = new LinkedList();
		
		
		try {
		BufferedReader lerArq = new BufferedReader(new FileReader("./src/list/exe1.txt"));
		Integer linha = Integer.valueOf(lerArq.readLine());
		
		while (linha != null) {
			lista.add(Integer.valueOf(linha));
			linha = Integer.valueOf(lerArq.readLine());  // lê da segunda até a última linha
		}
		
	} catch (
	IOException e) {
		System.err.printf("Erro na abertura do arquivo: %s.\n",
				e.getMessage());
	}
		
		System.out.printf("\nConteúdo da lista\n");
		for (int i = 0;i<=10;i++) {
		System.out.println(" "+lista.get(i));
	}
		
		System.out.println(); */
}
